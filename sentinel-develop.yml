http:

  port: 9018
  adminPort: 9019
  rootPath: /api/sentinel/*

logging:

  level: INFO

mySQLConfig:

  # The name of your JDBC driver
  driverClass: com.mysql.jdbc.Driver
  
  # The JDBC URL
  url: jdbc:mysql://localhost/sentineldb
 
  # The username
  username: sentinel
    
  # The password
  password: sentinel12345